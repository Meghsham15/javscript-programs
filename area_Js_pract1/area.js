class shape{
    areaRectangle(length,width){
        return length*width;
    }

    areaTriangle(base,height){
        return 0.5*base*height;
    }

    areaCircle(r){
        return Math.PI *(r*r);
    }
}